<%= form_with model: [:school_admin, @classroom], class: "form-grid" do |f| %>
  <% if @classroom.errors.any? %>
    <div class="flash flash-alert">
      <ul>
        <% @classroom.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, required: true %>
  </div>

  <div class="form-group">
    <%= f.label :status %>
    <%= f.select :status, status_options %>
  </div>

  <div class="form-group">
    <%= f.submit class: "btn btn-primary" %>
    <%= link_to "Cancel", school_admin_classrooms_path, class: "btn btn-secondary" %>
  </div>
<% end %>
